# strat of react js

npx create-react-app 01basicreact
run : cd 01basicreact ; npm start
for deploying full project : npm run build

in src : only keep index.js , app.js

vite project : npm create vite@latest
inside app.jsx : return (
<>
<h1>vite react app</h1>
<h1>hello world</h1>
</>
);

createing new file :
./src New-app.jsx(first letter capital )
then import into app.jsx
cd 01basicvite;
run: npm run dev
